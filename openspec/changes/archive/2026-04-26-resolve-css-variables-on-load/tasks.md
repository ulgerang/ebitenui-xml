# Tasks

- [x] Resolve variables before JSON style parsing through `UI.LoadStyles`.
- [x] Resolve variables before CSS rule parsing through `UI.LoadCSS`.
- [x] Add regression tests for resolved values and fallbacks.
- [x] Update docs/state.
- [x] Run validation.
